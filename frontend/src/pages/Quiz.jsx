function Quiz() {
  return (
    <div style={{ padding: '2rem' }}>
      <h1>Practice Quiz</h1>
      <p>Quiz generation will be wired in once the backend endpoints are ready.</p>
    </div>
  );
}

export default Quiz;
